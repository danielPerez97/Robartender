CREATE TABLE Recipe
(
    _id INTEGER AS Int,
    name TEXT NOT NULL,
    PRIMARY KEY (_id, name)
);

INSERT INTO Recipe VALUES(1, 'Screwdriver');
INSERT INTO Recipe VALUES(2, 'Mimosa');

selectAll:
SELECT * FROM Recipe;

selectByName:
SELECT * FROM Recipe WHERE name = ?;