CREATE TABLE IngredientRecipe
(
    I_ID INTEGER AS Int,
    R_ID INTEGER AS Int,
    PRIMARY KEY (I_ID, R_ID)
);

INSERT INTO IngredientRecipe VALUES(1, 1);
INSERT INTO IngredientRecipe VALUES(2, 1);
INSERT INTO IngredientRecipe VALUES(2, 2);
INSERT INTO IngredientRecipe VALUES(3, 2);

selectAll:
SELECT * FROM IngredientRecipe;

SELECT I_ID FROM IngredientRecipe ing
I